# Security policy

CaptureVault handles potentially sensitive screenshots. Please report security issues privately through GitHub's **Security** tab instead of opening a public issue.

The project is pre-release and does not yet publish a formal support window. Security fixes will target the latest `main` branch until the first stable release.
